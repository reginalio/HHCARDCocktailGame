<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hhcard.cocktailgame" >

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"       >
        <!-- Splash screen -->
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".MakeCocktailScreen"
            android:label="Make Cocktail"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PourLiquidScreen"
            android:label="Pour Liquid"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ShakeItScreen"
            android:label="Shake it"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".StirringScreen"
            android:label="Stirring"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Chosencocktail"
            android:label="Finish"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SetPreferenceActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
